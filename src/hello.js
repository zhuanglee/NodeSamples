const common = require('common-util');
const mongodb = require('mongodb-util');
const mysql = require('mysql-util');
const redis = require('redis-util');
const sql = require('sql-util');
const orm = require('orm-util');
console.log(common.getType(new Date()));
console.log('Hello world');